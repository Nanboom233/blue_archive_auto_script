version: "1.0"
profile:
  name: qodana.recommended
linter: qodana-python-community:2025.2
bootstrap: |
  python -m venv /data/project/.venv
  . /data/project/.venv/bin/activate
  pip install -r requirements-linux.txt
